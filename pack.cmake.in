file(COPY ${CMAKE_INSTALL_PREFIX}/aldi-pixel-font-${VERSION}.exe DESTINATION ${PROJECT_BINARY_DIR})
execute_process(COMMAND ${CMAKE_COMMAND} -E tar cfv aldi-pixel-font-${VERSION}.zip aldi-pixel-font-${VERSION}.exe --format=zip)
